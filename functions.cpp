#include<algorithm>
using namespace std;

bool cmp(int x,int y)
{
	//如果让x排在y前面，就返回true
	//否则返回false 
	return (x>y);
}

int main()
{
	min	//返回较小值 
	max
	swap	//交换两个元素
	
	sort(起始指针（包含）,结尾指针（不包含）,比较函数);
	
	
	
	int a[103],n;
	sort(a+1,a+n+1);//默认从小到大排序
	sort(a+1,a+n+1,cmp); 
	
	
	
	 log
	 
	 log(d)/log(c)
	 
	 
	 pow(a,b)//a^b
	 exp(b);//e^b
	 
	 sqrt(a);// = pow(a,0.5)
	 floor/ceil//下取整/上取整
	 sin/cos/asin/acos/tan/atan......
	 
	 
	 
	 strlen
	 
	 
	 strcmp
	 
	 strcpy
	 
	 
	 
	 
	 
	 
	 
	 
	 
	 
	  
	 
	 
	 
	 
	 
	 
	 
	 
	 
	 
	 
}
